import React from 'react'

const Header = (props) => (
    <div className="header">
        <h1>React Search!</h1>
        <p className="about">
            Here is a list of County Names rendered from a JSON object
        </p>
    </div>
)

export default Header